# NER LLM Backend

```bash
python -m pip install -r requirements.txt
python app.py
```

Defaults can be overridden with:

- `NER_LLM_MODEL`
- `NER_LLM_URL`
- `NER_MAX_TOKENS`
- `NER_BATCH_SIZE`
- `NER_REQUEST_TIMEOUT_SECONDS`
